{
  "name": "@stonewright/visual",
  "version": "0.1.0",
  "description": "Single-tool MCP workspace for nested WordPress editor adapters.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "files": ["dist", "LICENSE", "NOTICE"],
  "exports": { ".": "./dist/index.js" },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --outDir dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsup": "^8.5.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.9"
  }
}
