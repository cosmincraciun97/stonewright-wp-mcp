---
name: Landing page — Restaurant
description: Restaurant or hospitality landing with menu highlights, hours, and reservation CTA.
enable_agentic: true
enable_prompt: true
---

# Landing page — Restaurant

## Steps
1. Bootstrap task; confirm builder and theme.
2. Stock food/interior photos via Openverse (`stonewright/stock-image-search` / `import`) with attribution captions.
3. Sections: hero, signature dishes, about, hours/location, reservations.
4. Build with validated design spec; keep typography large and readable.

## Rules
- Never invent opening hours — ask or read site options.
- Menu items belong in structured content when possible.
