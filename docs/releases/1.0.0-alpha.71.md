# 1.0.0-alpha.71

## Highlights

- Discovery wiring: wave-3 admin ops and WooCommerce reads appear in `site-admin` / `content-model` profiles (plugin + companion fallback).
- Pluginless self-improvement: local skills and memory under `~/.stonewright/`, seven Direct tools including `stonewright-task-start`, delivered via MCP instructions; protocol e2e with zero WordPress credentials.
- ACF field group/value abilities, multi-plugin SEO meta (Yoast / Rank Math / AIOSEO / SEOPress), and CPT/taxonomy registration.
- Install prompts for plugin and Direct modes; wp-admin paste prompt is task-start-first.
- REST parity wave 3 merged earlier on this line (PR #5) with Codex security hardening (audit redaction, search filtering, read-only REST passthrough, etc.).

## Counts

| Surface | Value |
|---|---|
| Plugin abilities | **308** |
| Direct tools | **93** |

## Upgrade notes

- Direct installs can call `stonewright-task-start` without WordPress credentials for local skills/memory.
- `stonewright-rest-request` is **GET-only**; use typed tools or WP-CLI for writes.
- Prefer plugin mode for ACF field-group registration, CPT registration, Elementor, php-execute, and shared site skills.
