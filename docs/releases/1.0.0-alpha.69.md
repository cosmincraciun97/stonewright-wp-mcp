# 1.0.0-alpha.69

Pillar G: premium blueprints, tool surface, Elementor discipline, Direct blueprints, security notes.

## Abilities / surface

- Essential: blueprint-*, brand-kit-*, elementor-page-digest, elementor-build-tree, site-pulse, learning-record
- `stonewright/tool-profile` extras + tools_changed
- Settings: Allow HTML widgets (default off)

## Verify

```bash
cd plugin && composer test
cd ../companion && npm test && npm run typecheck && npm run build
```
