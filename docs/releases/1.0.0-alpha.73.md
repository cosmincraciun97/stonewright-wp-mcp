# 1.0.0-alpha.73

## Highlights

- **Premium finalization (PR #8):** admin polish, prompt library tab, real MCP profile switching, multi-engine blueprints (Elementor / Gutenberg / FSE), brand-kit preview + restore, DesignEvidence native-plan / CSS-gap gate, and interaction e2e.
- **FSE blueprint engine:** `engine=fse` with constrained layout, `EditorSnapshot`, and `FseTransactionQueue` apply + readback/rollback.
- **Authored DesignSpec v2 blueprints:** all 12 industry JSON packs ship with layout intent (`fullWidth`, `align_items`, `justify_content`), content facts, and native policy.
- **Elementor transactional full-tree writes:** `ElementorTransactionRunner` `replace_tree` / snapshot + structural readback on blueprint apply.
- **Brand kits:** `preview=true` diff mode and unconditional option/theme_mod `restore_id`.
- **Setup Apply now** for MCP surface with honest per-transport messaging.
- **Admin UI:** Prompt Library card grid CSS; blueprint action button spacing.
- **e2e:** blueprints, prompts, tooltips, connect, setup-profile, visual matrix, real blueprint-apply with REST session helpers.

## Counts

| Surface | Value |
|---|---|
| Plugin abilities | **312** |
| Direct tools | **98** |

## Upgrade notes

- Update the plugin **and** companion to alpha.73 together when using profile switching / `tools/list_changed`.
- Existing sites: MCP surface defaults remain; use Setup → **Apply now** after changing bootstrap/essential/full.
- Elementor path requires Elementor active for `engine=elementor`; otherwise pass `gutenberg` or `fse`.
- Brand-kit apply returns `restore_id` for option/theme_mod undo; Elementor kit posts still use post snapshots.
