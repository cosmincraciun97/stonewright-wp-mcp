# 1.0.0-alpha.68

Admin dark/light polish pass after live QA on alpha.67.

## Highlights

- Audit payload stays inside the table when expanded.
- Mode pill readable on light theme (header chrome).
- Blueprint Copy AI Prompt = full agent playbook.

## Abilities

No ability API changes. UI/prompt copy only for blueprints page.

## Verify

```bash
cd plugin && composer test
cd ../companion && npm test && npm run typecheck
```
